import { Link } from 'react-router-dom';
import Navbar from '../components/Navbar';
import { getStats } from '../services/gameService';

export default function Stats({ user, onLogout }) { const stats = getStats(); const cards = [{ key: 'hero', label: 'Héros', value: stats.hero, icon: '✦' }, { key: 'neutral', label: 'Anti-héros', value: stats.neutral, icon: '◈' }, { key: 'villain', label: 'Vilains', value: stats.villain, icon: '◆' }]; return <div className="app-shell stats-shell" style={{ '--page-image': 'url("/assets/power.webp")' }}><Navbar user={user} onLogout={onLogout} /><main className="stats-page"><span className="eyebrow">Les destins de la communauté</span><h1>Statistiques</h1><p className="lead">Chaque partie écrit une version différente de l’histoire.</p><div className="stats-grid">{cards.map((card) => <article className={`stat-card stat-${card.key}`} key={card.key}><span className="stat-icon">{card.icon}</span><strong>{card.value}</strong><span>{card.label}</span></article>)}</div><div className="stats-total"><span>Total de parties</span><strong>{stats.total}</strong><small>Les données sont actuellement mockées.</small></div><Link className="button button-secondary" to="/">← Retour à l’accueil</Link></main></div>; }
